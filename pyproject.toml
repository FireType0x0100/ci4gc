[tool.poetry]
name = "ci4gc"
version = "0.0.1"
description = ""
license = "MIT"
homepage = "https://github.com/FireType0x0100/ci4gc"
repository = "https://github.com/FireType0x0100/ci4gc"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
